# Script for debug builds
1. powershell script is made for windows and linux 64 bit(debugging is recommended for linux).
2. building with the powershell script is recomended
3. it is recommended to pull the docker image from git repo and use docker compose for the db and hanging in the folder for the config
WIP
